<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= title %></title>
		<link rel="stylesheet" href="/stylesheets/style.css" />
	</head>
	<body>
		<header>
			<span class="title">Mini Message Board</span>
		</header>
		<Main class="container">
			<button><a href="/new">Add new message</a></button>
			<ul>
				<% messages.map((message)=>{ %>
				<li>
					<h4><%= message.user %></h4>
					<h2><%= message.text %></h2>
					<small><%= message.added %></small>
				</li>
				<% })%>
			</ul>
		</Main>
	</body>
</html>
